<h1>Lista de Carritos</h1>
{{#each carts}}
  <div>
    <p>ID: {{this.id}}</p>
    <p>Productos:</p>
    <ul>
      {{#each this.products}}
        <li>{{this.product.name}} - Cantidad: {{this.quantity}}</li>
      {{/each}}
    </ul>
  </div>
{{/each}}